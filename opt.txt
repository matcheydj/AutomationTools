use strict;
use Getopt::Long;
 
 
my @libs    = ();
my %flags   = ();
my ( $verbose, $all, $more, $diam, $debug, $test, $step);
 
GetOptions(
        'verbose+'  => \$verbose,
         
);
 
 print "the verbose option is entered" if($verbose);
  